name: Archived Swift 6.4 refactor helper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Refactor already applied; retained only because automated deletion was blocked.'
